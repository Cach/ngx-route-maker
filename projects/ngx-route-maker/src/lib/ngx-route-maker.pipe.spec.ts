import { NgxRouteMakerPipe } from './ngx-route-maker.pipe';

describe('NgxRouteMakerPipe', () => {
  it('create an instance', () => {
    const pipe = new NgxRouteMakerPipe();
    expect(pipe).toBeTruthy();
  });
});
